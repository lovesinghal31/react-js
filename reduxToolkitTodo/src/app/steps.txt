1. configure store
2. create reducers (slice in redux toolkit)
    1. slice have name (different and meaning full names)
    2. initial state
    3. then reducers
        > in reducers we have to define whole function 
        > then export those indivisual funtionality